<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title or site.brand }}</title>
  <meta name="description" content="{{ description or site.description }}" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/assets/styles.css">
</head>
<body>
  <header id="header">
  <h1 id="logo">
    <a href="/">
      <span class="logo-mark">◎</span>  {# optional symbol / replace with your logo image if needed #}
      <span class="logo-text">INNERSTRONG</span>
    </a>
  </h1>
  <nav id="nav">
    <ul>
      {% for item in site.nav %}
        <li class="{% if page.url == item.url %}current{% endif %}">
          <a href="{{ item.url }}">{{ item.label }}</a>
        </li>
      {% endfor %}
    </ul>
  </nav>
</header>

  <main>
    {{ content | safe }}
  </main>

  <footer class="site-footer">
    <div class="container">
      <div class="socials">
        <a href="{{ site.facebook }}" target="_blank" aria-label="Facebook">📘</a>
        <a href="{{ site.instagram }}" target="_blank" aria-label="Instagram">📸</a>
        <a href="{{ site.tiktok }}" target="_blank" aria-label="TikTok">🎵</a>
      </div>
      <p class="micro">Cancel subscription anytime. Unsubscribe from emails anytime.</p>
      <p class="micro">© {% year %} {{ site.brand }}</p>
      <nav>
        <a href="/privacy">Privacy</a> · <a href="/terms">Terms</a>
      </nav>
    </div>
  </footer>
</body>
</html>
