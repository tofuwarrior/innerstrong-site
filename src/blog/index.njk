---
layout: layouts/base.njk
title: Blog | InnerStrong
description: Articles on relaxation, energy, focus, and nervous system mastery.
---
<section class="section">
  <div class="container">
    <h1>Blog</h1>
    <ul>
      {% for post in collections.post | reverse %}
        <li><a href="{{ post.url }}">{{ post.data.title }}</a> — <span class="micro">{{ post.date | date("yyyy-LL-dd") }}</span></li>
      {% endfor %}
    </ul>
  </div>
</section>
